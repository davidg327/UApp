import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  containerCards: {
    width: '90%',
    alignSelf: 'center',
  },
});

export default styles;
